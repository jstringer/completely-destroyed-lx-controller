msvc64/
bin/
module/lib/Release-x86_64/

# v0.2 run: deferred-questions log, force-added & pushed only at end of run
QUESTIONS.md
data/user_content.json

# runtime session sidecar (active-program state)
data/user_content.session

# agent bridge runtime files (src/lxagent.h)
.agent/

# ImGui/render window state written by the app every run (position, size, collapsed).
# Per-machine and per-session, so tracking it just produced churn and cross-machine conflicts.
.ini/

# release artifacts (scripts/release.py) -- built, not versioned
bin_package/
lxcontrol-*.zip
